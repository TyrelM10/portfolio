--> npm create vite@latest ./ -- --template react
--> npm install -D tailwindcss
--> npx tailwindcss init 
--> npm install --legacy-peer-deps @react-three/fiber @react-three/drei maath react-tilt react-vertical-timeline-component @emailjs/browser framer-motion react-router-dom
--> npm i three --save
--> Visit tailwindcss.com and read DOCS to understand the css better.
--> npm install --legacy-peer-deps -D tailwindcss postcss autoprefixer
--> npx tailwindcss init -p

--> npm run build
--> git add dist -f
--> git commit -m 'Adding DIST Again'
--> git subtree push --prefix dist origin gh-pages


# Changes to be made on next deploys:

***** 3. Try to get the entire background to star canvas for whole page.
4. Add Other Skills component with Music and other certificates.
5. Complete and add another project.
6. Add component for research papers.
7. Change color schemes finally before deploying and check ways to hide and encrypt the password and template info.